[project]
name = "wizard"
version = "0.1.0"
description = "Generative world model for cyber defense - predicts and blocks harmful AI agent trajectories"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.0.0",
    "transformers>=4.30.0",
    "pytest>=7.0.0",
]
